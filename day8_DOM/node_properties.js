
var boxElement = document.querySelector('.box');

console.log([boxElement]);

// atrributes: thuoc tinh cua element node 

// contenteditable: co the hiệu chỉnh nội dung của element node
// autofocus: tự động focus vào element node khi trang web được load
// autocalpitalize: tự động chuyển đổi chữ hoa thành chữ thường
// baseURI: trả về đường dẫn tuyệt đối của element node 
// childElementCount: trả về số lượng element con của element node
// childNodes: trả về một mảng các node con của element node bao gồm cả text node
// children: trả về một mảng các element con của element node nhung không bao gồm text node

// classList: trả về một mảng các class của element node
// firstChild: trả về node con đầu tiên của element node
// firstElementChild: trả về element con đầu tiên của element node
// lastChild: trả về node con cuối cùng của element node
// lastElementChild: trả về element con cuối cùng của element node
// nextSibling: trả về node kế tiếp của element node
// nextElementSibling: trả về element kế tiếp của element node
// previousSibling: trả về node trước đó của element node
// previousElementSibling: trả về element trước đó của element node
// parentElement: trả về element cha của element node
// parentNode: trả về node cha của element node
// nodeType: trả về kiểu của node 1: element node, 3: text node, 8: comment node 2: attribute node 
// nodeValue: trả về giá trị của node

// offsetHeight: trả về chiều cao của element node bao gồm cả padding và border
// offsetWidth: trả về chiều rộng của element node bao gồm cả padding và border
// offsetTop: trả về khoảng cách từ top của element node đến top của element cha gần nhất
// offsetLeft: trả về khoảng cách từ left của element node đến left của element cha gần nhất
// offsetParent: trả về element cha gần nhất mà có position khác static

// ownerDocument: trả về document mà element node đang thuộc về

// scrollHeight: trả về chiều cao của element node mà không bao gồm padding
// scrollWidth: trả về chiều rộng của element node mà không bao gồm padding
// scrollTop: trả về khoảng cách từ top của element node đến top của phần tử cuộn
// scrollLeft: trả về khoảng cách từ left của element node đến left của phần tử cuộn
// scrollIntoView(): cuộn trang web để element node hiện thị trên màn hình
// scrollIntoViewIfNeeded(): cuộn trang web để element node hiện thị trên màn hình nếu cần
// scrollByLines(): cuộn trang web theo số dòng
// scrollByPages(): cuộn trang web theo số trang
// scrollTo(): cuộn trang web đến vị trí cụ thể
// scrollIntoViewIfNeeded(): cuộn trang web để element node hiện thị trên màn hình nếu cần

// style: trả về một object chứa các thuộc tính css của element node
// tagName: trả về tên thẻ của element node
//spellcheck: kiểm tra chính tả của element node
// translate: dịch nội dung của element node
